# Firebase Configuration
# Copy this to .env.local and fill in your Firebase project values
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Gemini AI (free tier)
# Get your API key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Google Calendar Integration
# To enable Google Calendar sync:
# 1. Go to Google Cloud Console → APIs & Services → Enable "Google Calendar API"
# 2. Configure OAuth consent screen (add calendar.readonly scope)
# 3. The app uses Firebase's GoogleAuthProvider with additional Calendar scopes
# No extra env vars needed — Calendar OAuth uses the same Firebase Google sign-in
