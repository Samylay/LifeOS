import type { Metadata } from "next";

export const metadata: Metadata = { title: "Send to Claude — LifeOS" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
